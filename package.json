{
  "name": "add-style",
  "version": "1.0.0",
  "description": "Base Bootstrap file for Auto Data Direct, Inc.",
  "main": "index.js",
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "bootstrap": "^4.0.0-beta",
    "jquery": "^3.2.1",
    "node-sass": "^4.5.3",
    "nodemon": "^1.11.0"
  },
  "peerDependencies": {
    "bootstrap": "^4.0.0-beta"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node-sass --output-style expanded --source-map true --source-map-contents true --precision 6 scss/style.scss dist/add-style.css",
    "watch": "nodemon --watch scss --ignore dist/ -e scss -x \"npm run build\"",
    "build:dev": "node-sass --output-style expanded --precision 6 scss/dev.scss example/css/add-style.css",
    "watch:dev": "nodemon --watch scss --ignore dist/ -e scss -x \"npm run build:dev\""
  }
}
